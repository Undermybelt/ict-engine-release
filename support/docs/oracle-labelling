**⚠️ 研究提案草稿：手动 MECE regimes → 因子搜索 → HMM 标签恢复探针**

这个 pipeline 可作为**反向 top-down 的研究探针**（与上一个“先因子后聚类”正好相反），可近似归入 **“oracle labeling + HMM factor recovery”** 或 **Feature Saliency HMM** 一类思路。核心逻辑可作为研究流程参考，但不应直接视为生产路线：

1. **手动定义 MECE regimes**（互斥且穷尽）：用 ICT 领域知识把历史行情严格分成逻辑自洽、相互轮动的几类（例如：Bull-Trend、Bear-Trend、Range-Consolidation、Accumulation、Manipulation、Distribution）。这些类别必须覆盖全部数据且互不重叠，可通过规则或半手动标注实现。
2. **因子搜索**：迭代挑选/生成因子（你的 ICTFeatures 如 PDA 类型、FVG 大小、Liquidity Swept、Volume Imbalance、Silver Bullet 时间窗、Higher TF Bias 等），直到 HMM（或 HSMM）**在训练集上能近乎完美地恢复**你手动标注的 regimes（准确率 > 95% 或 posterior 匹配度极高）。
3. **验证与候选保留**：若标签恢复在 holdout 上仍稳健，可将对应因子集保留为候选研究特征；不得仅凭高恢复分数就直接锁定为生产特征，更不得直接替代现有 `timed-PDA / pre-bayes / voting` 真相面。

这个方法在你的 `ict-engine` 中**零成本集成**：已有 HMM + `ICTFeatures` + `PDASetup` + artifact ledger，只需在 `factor-research` 命令里新增 `--regime-discovery mece` 模式即可。

### 1. 完整复现思路（直接适用于 Rust CLI）
- **MECE 标注模块**：新增 `manual_mece_labeler`（rule-based + 可视化校验），基于你的 `detect_pda` 函数 + BOS/CHOCH + 时间窗等生成标签（e.g. 0=Bull-Trend, 1=Bear-Trend, 2=Range）。
- **因子搜索循环**：用 Feature Saliency / 包装法或简单 grid search 测试因子子集 → 跑 HMM → 计算标签恢复准确率（Viterbi 路径 vs 手动标签）。
- **成功标准**：HMM posterior / Viterbi 能“完美”匹配 MECE 标签（confusion matrix 对角线接近 1）。
- **输出**：锁定最优因子集 → 存入 `artifact ledger` → 生产 HMM 只用这组因子 → 驱动 CatBoost voting 和 `human-next` triage。
- **集成点**：`factor-research --regime-discovery mece` → 生成 `mece_regime_map.json` + `factor_recovery_report`（包含清晰的“哪些因子让 HMM 完美分类”的解释）。

### 2. 指导 Prompt（直接复制给 Claude / Grok / GPT-4o）
把下面整个 Prompt 丢给模型，后接你的 `ICTFeatures`、`PDASetup`、`EntryExecution` 定义 + 当前 HMM 代码，即可得到完整 Rust skeleton。

**Prompt 开始**

你现在是一位拥有 12 年 regime-aware 量化交易 + Rust HMM 经验的工程师，精通“手动 MECE regimes 定义 → 因子搜索 → HMM 完美恢复” pipeline。

项目：`ict-engine`（Rust CLI，已有 HMM + ICT PD Array 检测 + CatBoost voting + artifact ledger）。

**任务**：完整复现以下成功案例（纯架构 skeleton + 可复制结构，不写完整可运行代码）：

成功案例做法：
- 先手动将行情分为 MECE 的几个逻辑自洽、相互轮动的类别（e.g. Bull-Trend / Bear-Trend / Range / Accumulation 等）
- 然后迭代挑选因子（领域知识优先，如 PDA、FVG、Liquidity Swept、Volume Imbalance 等）
- 让 HMM（或 HSMM）在这些因子上**完全/近乎完美分类出**手动标注的 MECE 类别
- 一旦因子让 HMM 恢复标签成功率极高，就锁定这组因子用于生产 regime 检测

**具体要求**：
1. 在 `factor-research` 命令新增 `--regime-discovery mece` 参数。
2. MECE 标注模块：`mece_labeler(features: &ICTFeatures, candles: &[Candle]) -> Vec<i32>`（返回 0..N 的 regime 标签）。
3. 因子搜索模块：`search_factors_for_mece_recovery(...)`（迭代子集 → 跑 HMM → 计算 Viterbi 路径 vs 手动标签的准确率 / posterior match score）。
4. 成功判断：confusion matrix 对角线 > 0.95 或自定义 recovery score。
5. 锁定后：最优因子集存入 artifact ledger，生产 HMM 只用这组因子，输出 `regime_map` 直接喂给 `EntryExecution` 和 voting layer。
6. 与现有 workflow 融合：所有 MECE 标签 + 恢复报告存入 `reflection_bundle` 和 `workflow snapshot`。

输出格式（严格）：
- **整体架构 skeleton**（Rust 文件/模块建议）
- **每个模块的 trait / 函数签名 + 伪代码注释**
- **如何处理“因子不完美 → 继续搜索”循环**
- **与 CatBoost voting 的融合方式**
- **Rust 依赖建议**（linfa / 你的 hmmm crate / feature selection 扩展）

最后给出：如果我是技术 co-founder，这个 pipeline 如何分 3 个 sprint 落地。

只给建议和 skeleton，不写完整代码。

**Prompt 结束**

### 3. 支撑理论的论文（PDF 直链，最匹配你的 pipeline）
- **A novel dynamic asset allocation system using Feature Saliency HMM (FSHMM)**（2021，最核心：HMM 同时做因子选择 + regime 识别，完美匹配“找因子让 HMM 恢复 regimes”）  
  **PDF**：https://www.sciencedirect.com/science/article/abs/pii/S0957417420305443 （或通过 arXiv 搜索 FSHMM）  
  **匹配点**：嵌入式特征选择 + HMM 训练，自动挑选能区分 regimes 的因子。

- **A forest of opinions: A multi-model ensemble-HMM voting framework for market regime shift detection and trading**（2025，使用 oracle labeling + HMM 恢复标签）  
  **PDF/HTML**：https://www.aimspress.com/article/doi/10.3934/DSFE.2025019  
  **匹配点**：手动/oracle MECE-like 标签 → HMM 对齐 + 因子投票，exactly 你的流程。

- **Regime-Switching Factor Investing with Hidden Markov Models**（2020）  
  **PDF**：https://www.mdpi.com/1911-8074/13/12/311/pdf  
  **匹配点**：先定义 regimes → HMM 因子切换，理论基础坚实。

- **Market Regime Detection using Hidden Markov Models**（QuantStart 系列，含 manual-to-HMM 映射）  
  **链接**：https://www.quantstart.com/articles/market-regime-detection-using-hidden-markov-models-in-qstrader/

### 4. 可直接参考的 Repo（代码级复现）
- **Rust 最佳**：https://github.com/rustic-ml/FormicaX （Rust GMM/HMM 聚类 + feature selection，可扩展成 FSHMM）
- **Python 原型（最清晰 oracle labeling + HMM recovery）**：https://blog.quantinsti.com/regime-adaptive-trading-python/ （HMM + 手动/自动标签恢复）
- **FSHMM 实现参考**：搜索 “Feature Saliency HMM GitHub” 或 https://github.com/shortthirdman/Market-Regime-Detection （HMM + 因子迭代）
- **完整 regime labeling 示例**：https://datadave1.medium.com/detecting-market-regimes-hidden-markov-model-2462e819c72e （含手动特征到 HMM 的全流程）

把上面 Prompt 跑一遍，你就能得到**可直接复制进 `ict-engine` 的 Rust skeleton**，完美补全你的 HMM + BBN + 执行树闭环，让 `factor-research` 真正实现“MECE regimes 可被因子完美恢复”的生产级能力。

需要我把 Prompt 再精简成“只针对你的 ICTFeatures”的版本，或直接生成某个模块的 skeleton，随时说。