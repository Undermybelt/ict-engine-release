下面按你文档里的搜索原则筛过：优先 white-box、可解释、能落到 `offline trainer -> explicit artifact -> runtime read-only consume` 的论文/仓库；你原文把方向分成 Bundle A/B/C，我也按这个结构归类。
标题后的引用就是可点击链接。

## 先做的 4 个落地 Spike

| Spike                                    | 推荐资源                                                               | 怎么用                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1. Zero-config market data bootstrap** | OpenBB + OpenFIGI + Intake/Frictionless                            | 用 OpenBB 做 provider adapter，用 OpenFIGI 做 identifier mapping，用 Intake/Frictionless 生成 `data_catalog.json`、`symbol_resolution.json`、`normalized_dataset.parquet`。OpenBB ODP 面向统一接入 proprietary/licensed/public data source，OpenFIGI 提供 instrument identifier mapping，Intake/Frictionless 适合声明式 catalog 与数据描述/校验。([OpenBB][1]) |
| **2. Belief / path calibration**         | Open Bandit Pipeline + Vowpal Wabbit + Doubly Robust paper + MAPIE | 把 execution-tree 候选动作当 contextual bandit action，产出 `policy_correction_report.json`、`ope_ci.json`、`path_confidence_bounds.json`。OBP 提供 OPE estimator pipeline，VW 有 contextual bandit/OPE 教程，Doubly Robust 是核心论文，MAPIE 可做模型不确定性/区间。([GitHub][2])                                                                              |
| **3. Delayed reward + regime truth**     | lifelines/scikit-survival + ruptures + BOCPD + hmmlearn            | delayed outcome 不要直接算 naive win/loss；改成 `duration_posterior.json`、`hazard_summary.json`、`regime_posterior.parquet`。lifelines/scikit-survival 做 survival/time-to-event，ruptures 做 offline changepoint，BOCPD 做 online changepoint，hmmlearn 做 HMM regime baseline。([Lifelines][3])                                             |
| **4. White-box factor iteration**        | AlphaGen + PySR + Qlib + Alphalens/vectorbt                        | 用 AlphaGen/PySR 生成可读公式，Qlib 做训练/回测管线，Alphalens/vectorbt 做 factor grid 与 trade-density gate。产物应是 `factor_expression.json`、`factor_eval_grid.parquet`、`transfer_score.json`，不要直接把生成器接进 runtime。([GitHub][4])                                                                                                                  |

## 资源清单：按关键词映射

### Bundle A：用户启动 / 数据发现 / provider negotiation

| 关键词                                                                    | 论文/仓库                                  | 使用建议                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `zero-config market data bootstrap`, `provider capability negotiation` | **OpenBB Platform / ODP**              | 最适合作 provider abstraction 层。建议不要直接暴露 OpenBB provider 概念到 public CLI，而是写一个内部 resolver：`symbol + intent -> provider candidates -> normalized artifact`。([GitHub][5])                                                                |
| `historical dataset auto-discovery`, `data catalog inference`          | **Auctus**                             | Auctus 是 dataset search / augmentation platform，可索引不同来源的数据并查询可 join/append 的数据集。适合参考它的 “profile + index + query” 思路，做本地 market data catalog scorer。([Auctus Documentation][6])                                                    |
| `data discovery system`, `dataset resolver`                            | **Aurum**                              | Aurum 论文/系统重点是构建和查询 enterprise knowledge graph，用于 data lake/cloud/db discovery；适合借鉴成 `dataset_relationship_graph.json`，用于发现相似 symbol family 或可替代数据源。([MIT Debugging][7])                                                          |
| `semantic symbol matching`, `ticker normalization`                     | **OpenFIGI API + pytickersymbols**     | 先做 deterministic mapping：symbol、exchange、FIGI、ISIN、name alias。embedding/semantic matching 只放在 fallback，并输出置信度，不要作为唯一真值。OpenFIGI 支持 identifier mapping 和 keyword search；pytickersymbols 可从公开表生成 Yahoo/ISIN/name 映射。([OpenFIGI][8]) |
| `progressive disclosure CLI UX`, `adaptive first-run routing`          | **Progressive Disclosure paper**       | 可把 first-run router 设计成三层输出：默认下一步、为什么推荐、展开后显示高级候选。论文支持“先简化反馈，再逐步暴露透明度”的 UX 原则。([ACM Digital Library][9])                                                                                                                          |
| `artifact-first execution contracts`, `external runtime orchestration` | **Dagster + OpenLineage + MLflow/DVC** | Dagster 适合把数据集/模型/报告都建成 software-defined assets；OpenLineage 适合记录 run/job/dataset lineage；MLflow/DVC 可分别覆盖 model registry 与 data/model/pipeline versioning。([GitHub][10])                                                          |

### Bundle B：belief / calibration / delayed truth

| 关键词                                                                      | 论文/仓库                                                | 使用建议                                                                                                                                                                                        |                                                                                            |
| ------------------------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `contextual bandit`, `off-policy evaluation`, `doubly robust estimation` | **Doubly Robust Policy Evaluation and Learning**     | 这是 policy correction 的理论底座。把历史 execution path 当 logged bandit data：`context, chosen_action, propensity, reward, delay, candidate_policy_score`。产出 DR/SNIPS/IPS 对比报告，而不是只产单一胜率。([arXiv][11]) |                                                                                            |
| 同上                                                                       | **Open Bandit Pipeline**                             | 首选实验框架。它有 dataset preprocessing、policy learning、OPE estimators，适合快速搭 `policy_correction_report` 和 confidence interval surface。([GitHub][2])                                                 |                                                                                            |
| 同上                                                                       | **Vowpal Wabbit / vw-estimators**                    | 如果你需要更工程化、在线/离线都能跑的 contextual bandit baseline，用 VW；它文档里有 contextual bandit 和 offline policy evaluation。([Vowpal Wabbit][12])                                                               |                                                                                            |
| `structural path ranking calibration`, `conformal execution gating`      | **Conformal Risk Control + MAPIE/TorchCP**           | 用 conformal 方法给 ranker 输出“可执行/ abstain / 需要人工复核”的 gate。MAPIE 更适合 sklearn-style 快速接入；TorchCP 更适合深度模型/研究扩展。([arXiv][13])                                                                      |                                                                                            |
| `delayed reward replay validation`, `hazard calibration`                 | **lifelines / scikit-survival**                      | 适合把 delayed reward 变成 survival/hazard artifact：例如 `P(outcome by t                                                                                                                           | context, action)`。lifelines 上手快；scikit-survival 更贴合 sklearn pipeline 和 CV。([Lifelines][3]) |
| `changepoint-aware duration modeling`, `BOCPD`                           | **ruptures + Bayesian Online Changepoint Detection** | ruptures 用于离线 replay segmentation；BOCPD 用于 runtime-ish 的 regime/changepoint posterior，但建议仍产 artifact 给 runtime 只读消费。([Centre Borelli][14])                                                  |                                                                                            |
| `HMM regime continuity`, `semi-Markov model`                             | **hmmlearn / pyhsmm**                                | hmmlearn 适合作 baseline HMM regime posterior；pyhsmm 支持 explicit-duration HSMM，但仓库标注不再维护，建议只读论文/原型，不要作为生产依赖。([Hmmlearn][15])                                                                   |                                                                                            |
| `belief network evidence weighting`                                      | **pgmpy / pyAgrum / DoWhy**                          | pgmpy/pyAgrum 用于显式 Bayesian network evidence propagation；DoWhy 用于把 causal assumptions 写清楚并做 refutation，不建议直接把 causal score 当 trading truth。([GitHub][16])                                   |                                                                                            |

### Bundle C：factor generation / ranking / execution arbitration

| 关键词                                                             | 论文/仓库                                       | 使用建议                                                                                                                                                                                                                |
| --------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `white-box factor generation`, `symbolic factor search`         | **AlphaGen**                                | 最贴近“公式型 alpha + 可解释 artifact”。建议只用它产 candidate factor expression，再进入你的 factor-to-evidence pipeline 做 breadth、transfer、trade-density 检查。([GitHub][4])                                                                |
| `symbolic regression`, `explicit feature crafting`              | **PySR**                                    | 用于非金融专用的白盒公式搜索。建议把 operator set 限死，加入 complexity penalty，输出 `expression`, `complexity`, `fit_score`, `stability_score`。([GitHub][17])                                                                               |
| `factor-to-evidence pipeline`, `cross-market generalization`    | **Qlib**                                    | Qlib 覆盖 data processing、model training、backtesting、alpha seeking、risk modeling、portfolio optimization、execution，适合作 factor pipeline benchmark harness。([GitHub][18])                                                |
| `factor family breadth screening`, `trade-density-aware search` | **Alphalens + vectorbt**                    | Alphalens 做 IC、returns、turnover、grouped analysis；vectorbt 可批量测大量策略/参数。建议把“低交易次数”“单市场过拟合”“高 turnover”做成硬 gate。([Quantopian][19])                                                                                     |
| `CatBoost path ranking`, `LambdaMART`                           | **CatBoost ranking + LightGBM LambdaRank**  | CatBoost 的 YetiRank/Pairwise ranking 适合 explicit feature importance report；LightGBM LambdaRank 可作为强 baseline。输出应包括 `ranked_path_manifest.json`、`feature_importance.json`、`calibration_report.json`。([CatBoost][20]) |
| `execution-tree action arbitration`                             | **Learning-to-rank + conformal abstention** | 做成两段式：ranker 排序，conformal gate 决定执行/拒绝/降级。这样 public runtime 只需要读取 manifest，不需要知道内部 ontology。([CatBoost][20])                                                                                                        |

## 近期可以读但先别直接落地的研究

| 资源                           | 为什么值得看                                                                                                                      | 风险                                                                        |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **AlphaAgent**               | 用 LLM 生成 alpha，但加入 symbolic expression tree、hypothesis alignment、AST novelty 等约束，和你的 white-box artifact 方向有交集。([arXiv][21]) | LLM-driven factor generation 容易污染 evidence truth；建议只当 proposal generator。 |
| **Alpha-GPT**                | 交互式 alpha mining 系统，适合参考 researcher-in-the-loop UX。([ACL Anthology][22])                                                    | 不适合作无人值守 runtime。                                                         |
| **AlphaBench**               | 新的 formulaic alpha factor mining benchmark，适合评估 LLM/生成式方法。([OpenReview][23])                                                | benchmark 新，先用于离线比较，不要作为唯一判断标准。                                           |
| **Cross-Market Alpha paper** | 专门讨论 short-term trading factors 的 cross-market validation gap，贴合“cross-market generalization”。([arXiv][24])                 | 需检查数据和市场假设是否与你的目标市场一致。                                                    |

## 我建议你的搜索/落地顺序

先做 **Bundle A 的 resolver**：OpenBB + OpenFIGI + Intake/Frictionless，目标是把“手动选文件路径”消掉，产出稳定的 dataset artifact。然后做 **Bundle B 的 OPE/calibration**：OBP/VW + DR + survival/changepoint，目标是让 replay truth 和 delayed reward 更可信。最后做 **Bundle C 的 factor loop**：AlphaGen/PySR + Qlib + Alphalens/vectorbt + CatBoost ranking，目标是提高 factor throughput，但每个 factor 必须先过 breadth、transfer、trade-density、calibration gate。

[1]: https://openbb.co/products/odp/?utm_source=chatgpt.com "Open Data Platform | OpenBB"
[2]: https://github.com/st-tech/zr-obp?utm_source=chatgpt.com "GitHub - st-tech/zr-obp: Open Bandit Pipeline: a python library for ..."
[3]: https://lifelines.readthedocs.io/en/latest/?utm_source=chatgpt.com "lifelines — lifelines 0.30.3 documentation"
[4]: https://github.com/ICT-FinD-Lab/alphagen?utm_source=chatgpt.com "GitHub - ICT-FinD-Lab/alphagen: Generating sets of formulaic alpha ..."
[5]: https://github.com/OpenBB-finance/OpenBB?utm_source=chatgpt.com "GitHub - OpenBB-finance/OpenBB: Financial data platform for analysts ..."
[6]: https://docs.auctus.vida-nyu.org/?utm_source=chatgpt.com "Welcome to Auctus’s documentation! — Auctus v0.10-34-gce8a5242 ..."
[7]: https://mitdbg.github.io/aurum-datadiscovery/?utm_source=chatgpt.com "Aurum: Discovering Data in Lakes, Clouds and Databases"
[8]: https://www.openfigi.com/api/overview?utm_source=chatgpt.com "Overview - OpenFIGI"
[9]: https://dl.acm.org/doi/epdf/10.1145/3301275.3302322?utm_source=chatgpt.com "Progressive Disclosure - ACM Digital Library"
[10]: https://github.com/dagster-io/dagster?utm_source=chatgpt.com "GitHub - dagster-io/dagster: An orchestration platform for the ..."
[11]: https://arxiv.org/abs/1103.4601?utm_source=chatgpt.com "Doubly Robust Policy Evaluation and Learning"
[12]: https://vowpalwabbit.org/docs/vowpal_wabbit/python/latest/tutorials/python_Contextual_bandits_and_Vowpal_Wabbit.html?utm_source=chatgpt.com "Contextual Bandits — VowpalWabbit latest documentation"
[13]: https://arxiv.org/abs/2208.02814?utm_source=chatgpt.com "[2208.02814] Conformal Risk Control - arXiv.org"
[14]: https://centre-borelli.github.io/ruptures-docs/?utm_source=chatgpt.com "Welcome to ruptures - ruptures - GitHub Pages"
[15]: https://hmmlearn.readthedocs.io/en/latest/?utm_source=chatgpt.com "hmmlearn — hmmlearn 0.3.3.post1+ge01a10e documentation"
[16]: https://github.com/pgmpy/pgmpy?utm_source=chatgpt.com "Python Toolkit for Causal and Probabilistic Reasoning - GitHub"
[17]: https://github.com/MilesCranmer/PySR?utm_source=chatgpt.com "PySR: High-Performance Symbolic Regression in Python and Julia - GitHub"
[18]: https://github.com/microsoft/qlib?utm_source=chatgpt.com "GitHub - microsoft/qlib: Qlib is an AI-oriented Quant investment ..."
[19]: https://quantopian.github.io/alphalens/?utm_source=chatgpt.com "Alphalens — Alphalens 0.2.1+48.gad0be10 documentation - GitHub Pages"
[20]: https://catboost.ai/docs/en/concepts/loss-functions-ranking?utm_source=chatgpt.com "Ranking: objectives and metrics - CatBoost"
[21]: https://arxiv.org/html/2502.16789v1?utm_source=chatgpt.com "AlphaAgent: LLM-Driven Alpha Mining with Regularized Exploration to ..."
[22]: https://aclanthology.org/2025.emnlp-demos.14/?utm_source=chatgpt.com "Alpha-GPT: Human-AI Interactive Alpha Mining for Quantitative ..."
[23]: https://openreview.net/forum?id=d97Q8r7ZKZ&utm_source=chatgpt.com "AlphaBench: Benchmarking Large Language Models in Formulaic Alpha ..."
[24]: https://arxiv.org/abs/2601.06499?utm_source=chatgpt.com "Cross-Market Alpha: Testing Short-Term Trading Factors in the U.S ..."
